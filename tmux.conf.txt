set -g window-status-current-fg white
set -g window-status-current-bg red
set -g window-status-current-attr bold
set -g window-status-bg white
set -g status-bg blue

bind x set -g status-bg blue \; set -g prefix C-b
bind y set -g status-bg magenta \; set -g prefix C-y

bind c new-window -c "#{pane_current_path}"
bind | split-window -h
bind - split-window -v
bind r command-prompt "rename-window %%"
bind C-Left swap-window -t -1
bind C-Right swap-window -t +1
bind . source-file ~/.tmux.conf \; display-message ".tmux.conf reloaded"

bind u resize-pane -U 1
bind i resize-pane -D 1

set -g status-interval 1
set -g status-left "[#S] "
set -g status-right "[#H] %m/%d %H:%M:%S"

set -g history-limit 65536
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

set -g default-shell /bin/zsh
set -g default-terminal screen-256color
set -g terminal-overrides 'xterm:colors=256'
set-environment -g CHERE_INVOKING 1
